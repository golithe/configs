# gsd-xsettings fails its systemd Requisite= under Regolith's session target.
# Guard: i3 re-runs exec on restart, not on reload.
exec --no-startup-id pgrep -x gsd-xsettings >/dev/null || /usr/libexec/gsd-xsettings
